<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 项目坐标信息 -->
    <groupId>com.example</groupId>
    <artifactId>demo005-SpringAOP</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <!-- 项目名称和描述 -->
    <name>demo005-SpringAOP</name>
    <description>Spring AOP（面向切面编程）深入学习示例</description>

    <!-- Java 版本 -->
    <properties>
        <java.version>1.8</java.version>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Spring 版本 -->
        <spring.version>5.3.20</spring.version>
    </properties>

    <!-- 项目依赖 -->
    <dependencies>
        <!--
            spring-context: Spring核心容器
            提供了Spring最基本的IoC容器功能
            包含BeanFactory和ApplicationContext
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!--
            spring-aop: Spring AOP框架核心
            提供了AOP联盟接口的实现
            是Spring进行Aspect编织的基础
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aop</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!--
            aspectjrt: AspectJ运行时库
            提供了@Aspect等注解的运行时支持
            是Spring AOP能够识别@AspectJ注解的基础
        -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjrt</artifactId>
            <version>1.9.9.1</version>
        </dependency>

        <!--
            aspectjweaver: AspectJ编织器
            提供了切入点表达式的解析和匹配
            是@AspectJ注解能够生效的关键依赖
        -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
            <version>1.9.9.1</version>
        </dependency>

        <!--
            spring-aspects: Spring对AspectJ的集成支持
            提供了基于AspectJ的自动代理创建等功能
            用于支持更高级的AOP特性
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-aspects</artifactId>
            <version>${spring.version}</version>
        </dependency>
    </dependencies>

    <!-- Maven构建配置 -->
    <build>
        <plugins>
            <!-- Maven编译插件 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
