  # 1. 启动数据库
  cd springboot-projects && docker-compose up -d

  # 2. 执行SQL脚本创建表
  mysql -h localhost -u root -proot123 < sql/schema.sql

  # 3. 构建项目
  mvn clean install

  # 4. 运行前台服务
  java -jar web/target/web-1.0.0.jar

  # 5. 运行后台管理服务
  java -jar admin/target/admin-1.0.0.jar
