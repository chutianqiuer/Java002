seata:
  config:
    type: file
  registry:
    type: nacos
    nacos:
      application: seata-server
      serverAddr: nacos:8848
      group: DEFAULT_GROUP
      namespace: ""
      cluster: default
  store:
    mode: file
